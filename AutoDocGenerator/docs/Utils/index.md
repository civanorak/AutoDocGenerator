# Utils

> Auto-generated documentation for the **Utils** module of the Gorgon C++ Game Engine.


## Contents

- [Functions](#functions)

---

## Functions

### `CheckInputFor(const std::string &input, int &ch, P_ ...args)`

**Returns:** `int`


Checks if the input string contains one of the given characters at current point. If it does the index of the character found is returned and index is incremented. If not, an exception is thrown


### `for(int i = 0; i < elements; i++)`

**Returns:** ``



### `if(i != 0)`

**Returns:** ``



### `for(int i = 0; i < sizeof...(args)`

**Returns:** ``



### `if(allowed[i] == c)`

**Returns:** ``



### `ExtractQuotes(const std::string &input, int &ch)`

**Returns:** `inline std::string`


Extracts a string that is in quotes. This function supports both single and double quotes. Additionally, this function transforms following escape sequences \\", \\', \\n, and \\xx, where xx is an hexadecimal digit. Notice that \\0 is not valid, it should be given as \\00.


### `if(escape)`

**Returns:** ``



### `if(c == '"')`

**Returns:** ``



### `if(c == '\'')`

**Returns:** `else`



### `if(c == '\\')`

**Returns:** `else`



### `if(c == 'n')`

**Returns:** `else`



### `if(c == 't')`

**Returns:** `else`



### `if(c >= '0' && c <= '9')`

**Returns:** `else`



### `if(escapenum != 1)`

**Returns:** ``



### `if(escapenum == 2)`

**Returns:** ``



### `if(c == '\\')`

**Returns:** ``



### `if(c == '"' && quotes == 2)`

**Returns:** `else`



### `if(c == '\'' && quotes == 1)`

**Returns:** `else`



### `CheckInputFor(input, ch, quotes == 2 ? '"' : '\'')`

**Returns:** ``


